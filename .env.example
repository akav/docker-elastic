# Elastic Stack Environment Variables
# Copy this file to .env and customize as needed
# Usage: cp .env.example .env

# Elastic Stack Version
ELASTIC_VERSION=9.2.4

# Elasticsearch Configuration
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_USERNAME=elastic
# IMPORTANT: Change this password in production!
ELASTICSEARCH_PASSWORD=changeme

# Kibana Configuration
KIBANA_HOST=localhost
# Password for kibana_system user (required for Elastic 8+)
# Kibana cannot use the 'elastic' superuser - must use kibana_system
KIBANA_SYSTEM_PASSWORD=changeme
# Generate a new encryption key for production: openssl rand -base64 32
# Note: Avoid special characters (#, &, ], etc.) in the key for Windows compatibility
KIBANA_ENCRYPTION_KEY=aV67MfXdown18LNlA9Jt3kWuaC2xYz99

# Discovery Configuration
# single-node: For development/single instance (default)
# multi-node: For cluster deployment (requires additional configuration)
DISCOVERY_TYPE=single-node

# Logstash Configuration (if using GELF logging)
LOGSTASH_HOST=localhost

# Optional: Java heap settings (override defaults in docker-compose.yml)
# ES_JAVA_OPTS=-Xms4g -Xmx4g
# LS_JAVA_OPTS=-Xms1g -Xmx1g

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit your actual .env file to version control
# 2. Use strong passwords in production (minimum 16 characters)
# 3. Generate a unique KIBANA_ENCRYPTION_KEY for each deployment
# 4. Consider using Docker secrets for sensitive values in production
#
# To generate secure values:
#   Password: openssl rand -base64 24
#   Encryption Key: openssl rand -base64 32 | tr -d '/+='
